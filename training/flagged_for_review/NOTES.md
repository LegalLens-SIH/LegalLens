# Flagged-for-review log

Add one line per flagged image, per `training/LABELING_GUIDE.md` section G.2
(confident something is a legal declaration, unsure which of the 8 classes).
Review these together periodically and resolve each line to either a
confirmed class (fix the label, remove the line) or a decision to leave it
as best-guessed (remove the line, note the reasoning in
`training/CLASS_TAXONOMY.md` if it reveals a taxonomy gap worth recording).

Format: `image_filename.jpg — what's ambiguous and your best-guess class`

---

(empty - nothing flagged yet)
